# Configuraci?n del servidor
server.port=8084
# Configuración del servidor
server.port=8084

# Configuración de JWT
app.jwt.secret=secreto-por-defecto-que-debe-cambiarse-en-produccion
app.jwt.expiration-ms=86400000

# Configuración de log
logging.level.org.springframework.security=DEBUG
logging.level.com.dam.login=DEBUG

# Configuración de Spring Security
spring.security.user.name=admin@guzpasen.edu
spring.security.user.password=password
# Configuraci?n de la base de datos
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# Configuraci?n de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Configuraci?n de JWT
jwt.secret=claveSecretaParaFirmarTokensJWTGUZPASENProyectoTFGDAM2025
jwt.expiration=86400000

# Configuraci?n de logging
logging.level.org.springframework.security=DEBUG
logging.level.com.dam.login=DEBUG

# Configuraci?n de Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
