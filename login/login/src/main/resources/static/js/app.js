/**
 * Funciones comunes para la aplicación GUZPASEN
 */

// Función para volver atrás en la navegación
function goBack() {
    window.history.back();
}

// Inicializar componentes cuando el DOM está cargado
document.addEventListener('DOMContentLoaded', function() {
    console.log('Aplicación GUZPASEN inicializada');
});
